machine:
  node:
    version:  6.10.3
dependencies:
  override:
    - cd client; npm install
    - cd server; npm install
test:
  override:
    - cd client; npm run test
    - cd server; npm run test
